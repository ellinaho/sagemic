[project]
name = "sagemic"
version = "0.0.0"
requires-python = ">=3.9, <3.12"
dependencies = [
    "birdnetlib",
    "sounddevice",
    "scipy",
    "librosa",
    "tensorflow",
    "pyyaml"
]

[tool.setuptools]
packages = ["sagemic"]
